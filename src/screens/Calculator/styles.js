import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
  },
  screen: {
    height: '40%',
  },
  pad: {
    height: '60%',
  },
});

export default styles;

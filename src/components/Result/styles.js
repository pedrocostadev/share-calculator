import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  result: {
    fontSize: 28,
    color: '#FFFFFF',
    padding: 10,
    textAlign: 'right',
  },
});

export default styles;

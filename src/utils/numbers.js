export const numberOperations = {
  '+': (x, y) => parseFloat(x, 10) + parseFloat(y, 10),
  '*': (x, y) => parseFloat(x, 10) * parseFloat(y, 10),
  '/': (x, y) => parseFloat(x, 10) / parseFloat(y, 10),
  '-': (x, y) => parseFloat(x, 10) - parseFloat(y, 10),
};
